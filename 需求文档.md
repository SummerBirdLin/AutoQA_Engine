# 项目需求文档：屏幕局部自动答题与点击 Agent (Screen-QA-Agent)

## 1. 项目概述

### 1.1 背景与目标

本项目旨在构建一个运行于本地桌面的轻量级自动化答题 Agent。通过截取屏幕上指定的题目展示区域，利用本地轻量 OCR 引擎提取题目文字与选项的物理像素坐标，经由大语言模型（LLM）推理得出正确选项，最终换算坐标并通过鼠标驱动自动点击对应选项，实现低延迟、高准确率的全自动答题闭环。

本项目首先要针对的环境是 macOS 系统，而非 Windows。


### 1.2 核心指标

* **端到端耗时**：从触发截图到完成点击，耗时 $\le 2.5$ 秒。
* **点击准度**：针对标准 A/B/C/D 选择题，选项区域点击命中率 $\ge 98\%$。
* **本地资源占用**：轻量级运行，无显卡依赖（CPU 本地推理 OCR 耗时 $\le 100$ 毫秒）。

---

## 2. 系统架构与数据流

```
[屏幕指定区域]
       │  (mss 截图)
       ▼
[ROI 图像数据]
       │  (RapidOCR: 文字 + 四角坐标 Bounding Box)
       ▼
[结构化 OCR 数据列表]
       │
       ├─────────────────────────────────┐
       ▼                                 ▼
[提取题干与选项纯文本]              [保存选项字母与对应坐标映射]
       │                                 │
       ▼                                 │
[LLM 接口 (JSON Schema)]                 │
       │                                 │
       ▼                                 │
[目标选项 (如 "B")]                      │
       │                                 │
       └────────────────┬────────────────┘
                        ▼
            [计算目标中心逻辑坐标]
            (考虑 Windows DPI 缩放)
                        │
                        ▼
            [PyAutoGUI 模拟点击]

```

---

## 3. 功能需求

### 3.1 屏幕捕获模块 (Capture Module)

* **ROI（感兴趣区域）配置**：
* 支持在配置文件（`config.yaml`）中指定捕获矩形区域：`{left, top, width, height}`。
* 提供辅助标定脚本或热键模式：用户通过鼠标拖拽框选实时记录坐标与尺寸。


* **多屏与 DPI 感知**：
* 支持主副屏识别。
* 必须获取当前显示器的缩放比例（Scale Factor，如 125%、150%），确保截取的物理像素与系统逻辑像素能精确换算。


* **触发机制**：
* **手动触发**：全局热键（如 `F8`）唤醒执行单次答题。
* **轮询触发（可选）**：定时扫描指定区域，通过图像哈希（pHash）对比内容变化，检测到新题目时自动运行。



### 3.2 识别与坐标定位模块 (OCR & Localization Module)

* **引擎选型**：集成 `rapidocr_onnxruntime`，完全本地 CPU 运行。
* **数据结构输出**：
* OCR 识别结果必须保留每一段文本的**四角像素点坐标** `[[x1, y1], [x2, y2], [x3, y3], [x4, y4]]` 及置信度。


* **选项标号对齐**：
* 解析文本内容，提取选项标识（A / B / C / D 或 1 / 2 / 3 / 4）。
* 建立映射字典：`{ "A": (center_x, center_y), "B": (center_x, center_y), ... }`。
* 坐标原点需累加截屏区域在桌面上的全局偏移量 `(ROI_left, ROI_top)`。



### 3.3 模型推理模块 (LLM Reasoning Module)

* **API 兼容性**：遵循 OpenAI API 规范，支持接入 DeepSeek-V3、Qwen-Plus 等高性价比模型。
* **Prompt 约束**：
* 严格约束输出格式为 JSON，避免模型输出冗长解释。
* 针对单选与多选场景做判定。


* **Payload 示例**：

```json
// Prompt: 请根据以下题目与选项，直接输出正确答案字母。
{
  "question": "Python 中用于获取列表长度的内置函数是？",
  "options": [
    "A. size()",
    "B. length()",
    "C. len()",
    "D. count()"
  ]
}

// 期望输出格式:
{
  "answer": "C"
}

```

### 3.4 自动化执行模块 (Action Module)

* **坐标转换公式**：

$$\text{Target\_X} = \frac{\text{ROI\_Left} + \text{Box\_Center\_X}}{\text{Scale\_Factor}}$$


$$\text{Target\_Y} = \frac{\text{ROI\_Top} + \text{Box\_Center\_Y}}{\text{Scale\_Factor}}$$


* **鼠标点击动作**：
* 调用 `pyautogui.click(Target_X, Target_Y)`。
* 加入轻微随机偏移（$\pm 3$ 像素）及平滑移动耗时（0.05~0.15 秒），降低机械式点击特征。


* **紧急制动（Kill Switch）**：
* 开启 PyAutoGUI 内置安全机制 `pyautogui.FAILSAFE = True`（鼠标甩至屏幕四角立即终止）。
* 注册全局急停热键（如 `Ctrl + Q`），按下立即打断所有执行线程。



---

## 4. 异常处理与边界情况

| 异常场景 | 表现特征 | 容错策略 |
| --- | --- | --- |
| **选项未带字母** | 题目选项仅有文字，无 "A/B/C/D" 前缀 | OCR 按纵向坐标自上而下排序，强制按顺序映射为第 1、2、3、4 项传递给模型。 |
| **模型输出格式错误** | 模型返回了 Markdown 语法块或解释性文本 | 采用正则表达式提取第一个匹配的大写字母（如 `r'[A-D]'`）作为降级保底。 |
| **OCR 漏检目标选项** | 模型返回 "C"，但映射字典中不存在 "C" | 记录日志，弹出桌面 Toast 提示，放弃本次点击，严禁盲点误触。 |
| **网络请求超时** | 大模型 API 响应超过 3 秒 | 终止请求，释放鼠标焦点，等待下一次触发。 |

---

## 5. 技术选型与项目结构

### 5.1 依赖组件清单

* **编程语言**：Python 3.10+
* **屏幕截取**：`mss`（毫秒级抓屏，性能显著优于 PIL.ImageGrab）
* **文字与坐标识别**：`rapidocr_onnxruntime`
* **大模型调用**：`httpx` 或 `openai` 官方 SDK
* **自动化与输入**：`pyautogui`、`pynput` / `keyboard`
* **配置管理**：`pyyaml`

### 5.2 推荐工程目录结构

```text
screen_qa_agent/
├── config/
│   └── settings.yaml         # ROI 区域、API Key、热键等配置
├── core/
│   ├── __init__.py
│   ├── capturer.py           # 负责 mss 截图及 DPI 换算
│   ├── ocr_engine.py         # 封装 RapidOCR，返回文本及其中心坐标
│   ├── llm_client.py         # 封装大模型请求与 JSON 解析
│   └── executor.py           # 封装鼠标平滑移动与点击
├── utils/
│   ├── calibrate.py          # 辅助工具：用于拖拽框选确定屏幕 ROI 坐标
│   └── logger.py             # 日志记录
├── main.py                   # 主程序入口（注册热键与流程调度）
└── requirements.txt

```

---

## 6. 开发排期建议

1. **阶段一（坐标与捕获链路）**：完成 `calibrate.py` 框选工具与 `capturer.py`，打通截图到 DPI 坐标映射，确保点击绝对无偏差。
2. **阶段二（OCR 结构化与 LLM 串联）**：跑通 RapidOCR 文本框抓取，编写 Prompt 模板与正则降级逻辑。
3. **阶段三（状态闭环与急停机制）**：将热键监听、点击执行整合进 `main.py`，加入全局打断功能，进行多场景连贯测试。